import { PlaceholderPage } from "../../../src/components/PlaceholderPage";

export default function AnalyticsPage() {
  return (
    <PlaceholderPage
      title="Analytics"
      phase="Phase 9"
      description="PostHog funnels (onboarding → lesson → quiz → paywall) and the analytics_events table, joined against course/category data."
    />
  );
}

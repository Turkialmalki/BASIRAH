import { PlaceholderPage } from "../../../src/components/PlaceholderPage";

export default function SubscriptionsPage() {
  return (
    <PlaceholderPage
      title="Subscriptions"
      phase="Phase 7"
      description="RevenueCat plan configuration + the subscriptions table, populated by its webhook."
    />
  );
}

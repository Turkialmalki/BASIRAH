import { PlaceholderPage } from "../../../src/components/PlaceholderPage";

export default function UsersPage() {
  return (
    <PlaceholderPage
      title="Users"
      phase="Phase 9/10 (per-editor accounts + roles)"
      description="App user lookup/support tools, plus real per-editor admin accounts replacing the single shared access code (see middleware.ts)."
    />
  );
}

# Server-side only — never exposed to the browser (see src/lib/supabaseServer.ts).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# MVP shared-secret admin gate (see middleware.ts) — replace with real
# per-editor accounts before this has more than one editor.
ADMIN_ACCESS_CODE=

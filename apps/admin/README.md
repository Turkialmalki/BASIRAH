# @basirah/admin

Content CMS (Next.js + Tailwind + Supabase) — scaffolded in Phase 1, built in
**Phase 6** per the master build command (§23-24): scene editor (list /
mobile-preview / inspector three-pane layout), AI Content Studio, publish
workflow, analytics.

Not yet implemented. See `/docs/architecture.md` in the repo root for the
full route map (`/dashboard`, `/content`, `/courses`, `/scenes`,
`/ai-generator`, `/review`, `/publish`, `/analytics`, `/users`,
`/subscriptions`).
